<Window x:Class="SmartKasir.Client.Views.LoginView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        Title="SmartKasir - Login"
        Height="400"
        Width="500"
        WindowStartupLocation="CenterScreen"
        Background="{DynamicResource MaterialDesignPaper}"
        TextElement.Foreground="{DynamicResource MaterialDesignBody}"
        TextElement.FontWeight="Regular"
        TextElement.FontSize="13"
        TextOptions.TextFormattingMode="Ideal"
        TextOptions.TextRenderingMode="Auto">

    <Grid>
        <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center" Width="350">
            <!-- Header -->
            <TextBlock Text="SmartKasir POS" 
                       FontSize="32" 
                       FontWeight="Bold" 
                       Foreground="{DynamicResource PrimaryHueMidBrush}"
                       HorizontalAlignment="Center"
                       Margin="0,0,0,30"/>

            <!-- Username -->
            <TextBlock Text="Username" Margin="0,0,0,8"/>
            <TextBox x:Name="UsernameTextBox"
                     materialDesign:HintAssist.Hint="Masukkan username"
                     Padding="12,8"
                     Margin="0,0,0,16"/>

            <!-- Password -->
            <TextBlock Text="Password" Margin="0,0,0,8"/>
            <PasswordBox x:Name="PasswordBox"
                         materialDesign:HintAssist.Hint="Masukkan password"
                         Padding="12,8"
                         Margin="0,0,0,16"/>

            <!-- Error Message -->
            <TextBlock x:Name="ErrorMessageBlock"
                       Text="{Binding ErrorMessage}"
                       Foreground="Red"
                       TextWrapping="Wrap"
                       Margin="0,0,0,16"
                       Visibility="{Binding ErrorMessage, Converter={StaticResource StringToVisibilityConverter}}"/>

            <!-- Login Button -->
            <Button Content="Login"
                    Height="40"
                    FontSize="14"
                    Background="{DynamicResource PrimaryHueMidBrush}"
                    Foreground="White"
                    IsEnabled="{Binding IsLoading, Converter={StaticResource InvertBoolConverter}}"
                    Margin="0,0,0,16"/>

            <!-- Loading Indicator -->
            <ProgressBar IsIndeterminate="True"
                         Height="4"
                         Visibility="{Binding IsLoading, Converter={StaticResource BoolToVisibilityConverter}}"
                         Margin="0,0,0,16"/>
        </StackPanel>
    </Grid>
</Window>
